let input = document.getElementById("searchInput");input.addEventListener('input', function () {    let filter = input.value.toUpperCase();    let courseCards = document.getElementsByClassName("col-lg-4 col-md-6 mb-5");    let noResultMessage = document.getElementById("noResultMessage");    // Biến đếm số lượng khóa học hiển thị    let visibleCount = 0;    // Lặp qua từng thẻ chứa khóa học    for (let i = 0; i < courseCards.length; i++) {        let courseName = courseCards[i].getElementsByClassName("card-title")[0];        let topic = courseCards[i].getElementsByClassName("card-text")[0];        if (courseName && topic) {            let txtValueName = courseName.textContent || courseName.innerText;            let txtValueTopic = topic.textContent || topic.innerText;            if (txtValueName.toUpperCase().indexOf(filter) > -1 || txtValueTopic.toUpperCase().indexOf(filter) > -1) {                courseCards[i].style.display = "";                visibleCount++;            } else {                courseCards[i].style.display = "none";            }        }    }    // Hiển thị hoặc ẩn thông báo khi không có kết quả    if (visibleCount === 0) {        if (!noResultMessage) {            noResultMessage = document.createElement("div");            noResultMessage.id = "noResultMessage";            noResultMessage.className = "alert alert-warning mt-5";            noResultMessage.textContent = "Không tìm thấy kết quả.";            document.getElementById("courseSection").appendChild(noResultMessage);        } else {            noResultMessage.style.display = "block";        }    } else {        if (noResultMessage) {            noResultMessage.style.display = "none";        }    }});